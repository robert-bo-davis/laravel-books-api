<?xml version="1.0"?>
<ruleset name="laravel-books-example">

    <description>Almost PSR2</description>

    <!-- do not lint these directories -->
    <exclude-pattern>/vendor</exclude-pattern>
    <exclude-pattern>/app/bootstrap/cache</exclude-pattern>
    <exclude-pattern>/database</exclude-pattern>
    <exclude-pattern>/storage</exclude-pattern>
    <exclude-pattern>/public/docs</exclude-pattern>


    <!-- include most of the predefined psr2 ruleset -->
    <rule ref="PSR2">
        <!-- exclude rules we are unable to comply with yet, such as required namespacing -->
        <!-- <exclude name="PSR1.Classes.ClassDeclaration"/>
        <exclude name="Generi -->c.Files.LineLength"/>
    </rule>

    <!-- additional rules -->
    <rule ref="Generic.Arrays.DisallowLongArraySyntax"/>

</ruleset>
